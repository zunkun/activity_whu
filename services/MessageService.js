class MessageService {
	/**
   * 创建活动成功发给审核
   * @param {Number} activityId 活动ID
   * @param {Object} users 接受者信息
   * @param {String} userId 接手者userId
   */
	static async sendReviewer (activityId, users, userId) {

	}
}

module.exports = MessageService;
